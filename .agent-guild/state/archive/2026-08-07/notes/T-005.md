# T-005 self-report

## What I changed

Two files, both under `_working-memory/`. Nothing else in the tree was touched.

### `conventions.md`, Error Handling

The old single bullet bundled two unrelated exceptions to `set -e`. I split it, because only one of them was still true.

- The `set -e` bullet keeps the macOS-defaults exception and drops the installer half. I replaced the parenthetical "(non-fatal `defaults write` errors)" with the reason from decisionLog 2026-03-24, that one failing key shouldn't abort the whole run.
- A new bullet states the rule this job establishes: installers never guard an install call with `|| true`; absence prints a skip line naming the tool; a tool that ran names each item it couldn't install in an end-of-run summary, with its stderr left on screen; both paths exit 0.
- A third bullet carries the Raycast ceiling, that a site reports only what it can observe.

### `conventions.md`, bats bullet

Kept "don't reach in for helper functions" verbatim in substance. Qualified the wording ban to "as a stand-in for behavior" and added the carve-out for a script whose output is the contract, naming `tests/install-failures.bats`.

### `decisionLog.md`

New entry at the top, dated 2026-08-06, in the file's **Context** / **Decision** / **Alternatives considered** shape with the same bolded mid-entry lead-ins the neighbouring entries use.

## Taste calls

**Three bullets, not one, in Error Handling.** The task asked me to preserve the `run_once_after_configure-macos.sh` half of the old bullet. Rewriting one bullet to carry a dead convention's replacement plus a live unrelated exception would have produced a sentence with two subjects and no rule in it. Splitting is what let each bullet state one rule.

**Dated the entry 2026-08-06, not 2026-08-07.** The incident, the decision, and the rest of the job's state all sit on 08-06, and the entries directly beneath it are the same day. It's newest-at-top within that day.

**Em dashes.** `~/.claude/CLAUDE.md` forbids spaced em dashes; these two files use them 84 times and unspaced ones zero times. I resolved it by cutting em dashes out of my freeform prose entirely and keeping them only in the `Alternatives considered:` line, where `X — rejected, why` is a structural idiom every entry in the file already follows. Net new spaced dashes: three, all inside that idiom.

**Register over lint.** Markdownlint fires MD022/MD032 on every heading and list in both files, my additions included. It's the files' existing style, so I matched it rather than reformatting around a warning I'd be introducing inconsistency to silence. I also converted my "two traps" sub-list into the bolded-paragraph form the rest of `decisionLog.md` uses, since no other entry carries a nested list.

## Humanizer

Invoked the skill and revised against its audit. What it actually caught:

1. **A fabricated number.** I'd written that one failing `defaults write` key "shouldn't take the other hundred down." Nothing sourced that. `run_once_after_configure-macos.sh` has 42 `defaults write` calls, so I cut the count rather than correct it.
2. **An inaccurate claim in Context.** My first draft said an absent Homebrew and a failed `fzf` install "reached the log as the same sentence." False for `install-packages` specifically, which has always printed a distinct not-found message; its actual defect is that `|| echo "Some Brewfile entries failed"` reads identically whether an entry was unavailable or a MAS app was waiting on a password. Rewrote to separate the three `|| true` sites from the one `|| echo` site.
3. Two rule-of-three constructions where the first two items restated each other, an aphoristic "print the same nothing," and a filler tail on the "two traps" lead-in. All trimmed.

## Verification

No protected-passages manifest exists for this job (constitution, "Protected content: None"), so `check-protected.py` had nothing to run against.

Every factual claim in both files was re-derived from the artifacts rather than from the task prompt: the four converted scripts for the output contract and the raycast `defaults write` placement, `tests/install-failures.bats` for what its assertions actually pin (the item name plus a fragment of the surrounding line, via `grep -qF` helpers), and `/bin/bash --version` for 3.2.57.

## Out of scope, deliberately

Did not touch the four `run_onchange_*` templates, anything under `tests/`, or `activeContext.md`. Did not commit. `git status` shows exactly two modified paths under `_working-memory/`.
