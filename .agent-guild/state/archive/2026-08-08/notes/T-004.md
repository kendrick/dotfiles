# T-004 self-report

Converted all 11 bare `[[ ]]` assertions in `tests/apps.bats` to `assert_contains` calls. The file's
`load 'helpers'` line (added by T-001) was already present in the working tree, so no baseline-line
offset math was needed — I located each site by its text as instructed.

## The six `$output` sites (single-argument form)

- `found 0`
- `nothing untracked`
- `fzf`
- `unknown option`
- `--adopt`
- `--machine`

## The five `registry_line` sites (explicit haystack, needle kept single-quoted verbatim)

- `assert_contains 'bundles = ["media"]' "$(registry_line 'owner/repo/x')"`
- `assert_contains '["cloud"]' "$(registry_line spotify)"`
- `assert_contains '["cloud"]' "$(registry_line 1password)"`
- `assert_contains 'bundles = ["core", "media"]' "$(registry_line spotify)"`
- `assert_contains '{ name = "yq", type = "brew", bundles = ["core"] },' "$(registry_line yq)"` — kept
  the trailing comma per the task file's instruction not to tidy it away.

Left the `grep -oE '\{ name = "[^"]+", *type = "[^"]+", *bundles = \[[^]]*\] \}'` line (the twelfth
`[[` hit from a naive grep) completely untouched, since it's a regex inside `chezmoi`'s stub, not an
assertion.

## Verification run

```
$ /usr/bin/grep -nE '^[[:space:]]*(\[\[|\(\()' tests/apps.bats
(no output, exit 1)

$ /usr/bin/grep -c '^@test' tests/apps.bats
18

$ bats tests/apps.bats
1..18
ok 1 moves the selected package and leaves its alignment and comment alone
... (all 18 ok, none skipped)
```

No caveats — all five tricky `registry_line` conversions matched the task file's literal text
exactly, and the suite runtime was fast (no fzf-stub hang). Touched only `tests/apps.bats`; did not
commit, push, or run `chezmoi apply`.
