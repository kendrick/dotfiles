# T-005 self-report

## What I did

Converted all 11 bare `[[ ]]` sites across `tests/jsonc.bats` (2), `tests/licensed-fonts.bats` (5), and `tests/packages.bats` (4, including the guard). Located every site by its text rather than by baseline line number, since T-001's `load 'helpers'` line shifted everything by +2.

- `tests/jsonc.bats`: both `== *"while parsing"*` sites -> `assert_contains "while parsing"`.
- `tests/licensed-fonts.bats`: `== *"not installed"*`, `== *"not signed in"*`, `== *"would not authorize"*` -> `assert_contains`; the `!= *"not in vault"*` immediately below the `would not authorize` check -> `assert_not_contains "not in vault"` (kept polarity distinct from the `assert_contains "not in vault"` a few lines later, since those two assert opposite things about neighboring output); final `== *"not in vault"*` -> `assert_contains "not in vault"`.
- `tests/packages.bats`: `== *'brew "age"'*` -> `assert_contains 'brew "age"'` (single-quoted call, needle keeps its embedded double quotes); `!= *"databricks"*` and `!= *"font-lilex"*` -> `assert_not_contains`.
- `tests/packages.bats:146` (baseline number; now at 148 in the working tree): `[[ -n "$cask" ]] || continue` -> `[ -n "$cask" ] || continue`, single-bracket, matching the idiom already used at `tests/font.bats:552` (`[ -n "$fallback" ] || continue`). Converted, not deleted, per the task's explicit warning that deletion would pass the syntax check while silently breaking the loop's blank-line skip.

## Left untouched, as instructed

- `tests/packages.bats:105` (now ~107) — `[ "$(grep -c '^[[:space:]]' <<<"$output")" -eq 0 ]` — a `[[:space:]]` POSIX character class inside a single-bracket test, not a `[[` command. Confirmed still present verbatim.

## Verification run

```
/usr/bin/grep -nE '^[[:space:]]*(\[\[|\(\()' tests/jsonc.bats tests/licensed-fonts.bats tests/packages.bats
```
Exit 1, no output (confirmed with `echo "grep exit=$?"` printing `1`).

```
bats tests/jsonc.bats tests/licensed-fonts.bats tests/packages.bats
```
`1..29`, all 29 `ok`. Case counts match the spec's floors: jsonc 9, licensed-fonts 7, packages 13.

## Scope

Touched only the three assigned files. Did not touch `doctor.bats`, `font.bats`, `apps.bats`, `helpers.bash`, or anything under `dot_local/`. Did not commit or run `chezmoi apply`. `git diff --stat` shows exactly the three files, with only assertion-line and guard-line changes (the `load 'helpers'` lines already present from T-001 are untouched by my edits, just visible in the diff context).

## Judgment calls

None beyond what the task spelled out. The needle/polarity mapping in `licensed-fonts.bats` around lines 62-63 (baseline) was exactly as risky as flagged — double-checked that `would not authorize` kept `assert_contains` and the adjacent `not in vault` kept `assert_not_contains` in that same test, not swapped.
