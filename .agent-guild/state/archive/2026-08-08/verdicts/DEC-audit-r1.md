---
task: DEC-audit
checker: auditor
vendor: anthropic
model: claude-opus-5
verdict: PASS
checked_at: 2026-08-08T21:15:00Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| audit-correction | info | You are right and I was wrong. I quoted B-2 as reading "**exactly 52 assertions call a shared helper** — not 'at least 52'" and cited it as constitution.md:191. The current text reads "So **52 of the 53 are single helper calls**, and all 53 are mutated by V-2", followed by a paragraph ending "**That implementation is correct and must not be failed on the count.**" Nothing is stale in your copy — the stale copy was mine. I read the constitution during CON-audit r3, quoted that line from memory of that read during DEC-audit, and never re-opened the file, despite your dispatch note saying it was "revision 4 plus the folded-in amendments". That is precisely the failure mode I failed this document for three rounds running: asserting a measurement I had not taken. The count leg of blocker 1 was already closed before DEC-audit ran and I should not have raised it. The other two legs stood on their own — V-2 part 2 pins doctor at exactly 20, and both mutation rules would have claimed a helper-built `if` — and those are what your fix addresses. Recording the correction rather than quietly dropping it, which is this document's own house rule. | constitution.md:191 now reads `So **52 of the 53 are single helper calls**, and all 53 are mutated by V-2.` constitution.md:193 reads `Counting the 53rd is a judgment call, not an arithmetic one: the better implementation of doctor.bats:244 builds its if out of helper calls, which a literal checker would count as 53 helper-calling assertions rather than 52. **That implementation is correct and must not be failed on the count.** What the number is guarding is the floor — no assertion may inline its own comparison — not a ceiling on how many call a helper.` File mtime 2026-08-08 12:24:29, ahead of the DEC-audit dispatch. The two legs that stood: constitution.md:300 (`apps 11, doctor 20, ...`) and constitution.md:278-279 (the two mutation rules). |
| T-009 | major | Answering question 4, and it is the one thing nobody has looked at twice. **T-009 never says what to do if the assembled suite is red, and its scope lets it fix the problem in the one way that escapes B-2.** The task's Scope paragraph permits the diff to touch `tests/`, and its clauses are V-3, D-1, D-3 and now B-1 — **not B-2**. So an opus worker told to "confirm the suite is green and in scope, then commit", finding one converted assertion failing, will most naturally repair the conversion: it is inside its stated scope, it makes the check pass, and nothing tells it not to. That edit is then the only conversion in the job never read against its baseline by a B-2 judgment checker, because B-2's checker ran two waves earlier on a version of the file that no longer exists. The job's meaningfulness gate has a door in it at the last task. The two guardrails present do not cover this: "If a conversion is stubborn, the conversion is wrong; deleting its case is the failing example this clause exists to catch" forbids deleting a case, and "if you find yourself editing a script under test to make an assertion pass, stop and report it" is about `dot_local/`, which is already out of scope. Neither forbids editing the converted assertion itself. One sentence closes it: if the assembled suite is red, stop and report rather than repairing a conversion — a conversion fix belongs to the task that owns that file, through rework, so B-2's checker sees it. T-009 is `pending` and two waves out, so this costs nothing to fix now. | T-009.md:67 (Scope, permitting `tests/`), T-009.md:5 (`clauses: [V-3, D-1, D-3, B-1]`), T-009.md:50 (the deletion guardrail), T-009.md:67 (the `dot_local/` guardrail). Against constitution.md:194, whose B-2 check is a reading of 53 before/after pairs against `git show 61213fe:<file>` — an instrument that only runs inside T-002..T-005's checks. |
| T-008 | minor | T-008 is the only one of the nine tasks with no "Verify before reporting done" block, and it is a task that edits files on the guarded surface. It reviews and rewrites comments across seven `.bats` files plus `tests/helpers.bash`, and its checker is `checker-judgment` on D-2 — an instrument that reads prose and never runs bats. So the first thing that would catch a slip in those edits (a mangled heredoc, a line lost while moving a comment) is T-009's `timeout 300 bats tests/`, one task later, where the diagnosis arrives detached from its cause. Every other task in the job ends with a verify block; T-008 should too — `bats tests/` still green, and `git diff --stat` confined to comments plus the two working-memory files. Nothing is uncovered here, since T-009 does eventually run the suite and its new B-1 sweep does cover install-failures.bats; this is diagnosis cost and a consistency gap rather than a hole. | T-008.md ends at :51 with the `**Do not** touch any file outside...` paragraph; `/usr/bin/grep -i 'verify before\\\|bats tests' T-008.md` -> no match. Compare T-001.md:60-66, T-002.md:74-80, T-003.md:83-89, T-004.md:77-83, T-005.md:88-93, T-006.md:59-63, T-007.md:59-68, T-009.md:36-40, all of which carry one. T-008.md:21 (`deps: [T-006, T-007]`) and T-008.md:45 (`If a comment an earlier task wrote is weak, improving it is in scope`). |
| T-008 | minor | T-008 and T-009 are handed opposite instructions about the issue's "24 vulnerable" figure, and only one of them is told why. T-008 is told not to restate it, with the reason given: it counts only assertions with a following statement in their block, misses the loop case, and no corrected number was ever measured. T-009 is then handed `24-plus assertions may have been checking nothing` as suggested commit-message copy. The phrasing is defensible — if 24 is an undercount then "24-plus" is true, and the hedge is accurate since a `[[ ]]` in a vulnerable position genuinely cannot fail its test — and the two artifacts do serve different readers. But if the reason for keeping the number out of working memory is that it was never measured, the same reason reaches a commit message someone reads cold a year from now, which is exactly the audience T-009's own message rubric is written for. Worth one decision applied to both rather than two instructions that read as contradictory to a worker who sees only one of them. | T-008.md:47 (`Do not restate the issue's "24 vulnerable" figure — it is an undercount, because it counts only assertions with a following statement in their block and misses the loop case, and no corrected number was ever measured`) against T-009.md:57 (`the why is that a false assertion was reporting ok and 24-plus assertions may have been checking nothing`). constitution.md:93 and constitution.md:352 both decline to state a corrected total. |
| T-002 | info | Answering question 1: the fix removes the ambiguity rather than relocating it, and it closes for all three consumers independently. Walked each. **B-2's checker**: `doctor.bats:244` is exception 2, so the "inlines its own comparison" FAIL condition does not reach it, and the amended count paragraph means neither form is failed on arithmetic — a plain `case` block passes. **V-2 part 2**: with `:244` as a `case` block rather than two helper calls, an enumerator finds 19 helper-call sites plus one special site in `doctor.bats` = exactly 20, which is what the check pins. **The two mutation rules**: rule 1 keys on the helper name and no longer matches, rule 2 (sentinel needles, located by `@test` name and needle strings) matches uniquely, so the collision is gone and the script has one defined behaviour at that site. T-002 also states all three reasons in the task rather than issuing a bare prohibition, and is explicit that the constitution permits either form — so a worker who wants to deviate knows exactly what it would cost, which is the right shape for a constraint that comes from a downstream artifact rather than from the clause the task cites. The sample `case` block is correct: both substrings survive, polarity is preserved, `return 1` fails the test from any position, and its first token is `case`, so B-1 is satisfied. | T-002.md's disjunction section against constitution.md:189 (exception 2), constitution.md:193 (the count paragraph), constitution.md:300 (doctor 20), constitution.md:278-279 (the two rules), and T-007.md's `Locate it by its enclosing @test name and its needle strings`, which is form-agnostic and so handles either `case` or `grep`. Baseline site verified: tests/doctor.bats:244 is the suite's only disjunction, and doctor.bats holds exactly 20 first-token sites at 61213fe. |
| T-007 | info | Answering question 2: the boundary is airtight under the new wave order, and — better — it is not held up by the task file alone. I enumerated everything under `tests/` that could carry a helper call when T-007 runs. The six conversion files: enumerated by name. `install-failures.bats`: excluded by name, with its 30 pre-existing calls and their provenance stated. `lint.bats`: does not exist yet, because T-006 now runs after T-007, **and** excluded by name anyway — belt and braces, and the belt is the one that matters since it survives a future reordering. `helpers.bash`: holds definitions, not calls, and a definition does not match a call-shaped pattern (`assert_contains() {` has `(` where a call has whitespace); it is not a `.bats` file and is not listed. `mutation-check.sh`: the script itself, not listed. Nothing acquires a call later either — T-008's edits are comments, and a `#`-prefixed line cannot be a first-token call. The reason this is stronger than an instruction is that V-2 part 2's exact per-file counts are the check behind it: a script that widens its glob lands on 82-plus and fails loudly on its first run rather than quietly over-reporting. | T-007.md's enumeration section against measurement at 61213fe: install-failures.bats -> 30 helper call sites, all six conversion files -> 0. Dep graph now T-006: [T-002, T-003, T-004, T-005, **T-007**]; T-007: [T-002..T-005]. constitution.md:300 for the per-file counts that enforce the boundary. |
| T-006 | info | Answering question 3: the new edge causes no problem in the reverse direction, and the ordering is now forced in the only direction that works. When T-007 runs, `lint.bats` being absent is harmless — V-2 part 2 counts only the six conversion files, and T-007's B-1 check names a single file that exists by then. When T-006 runs, all three members of the guarded surface exist, so its guard scans real files rather than tripping the `grep` exit-2 path, and V-1's planting 3 has a real `mutation-check.sh` to plant in and a real one to `cp` back from. Nothing in T-007's checks needs `lint.bats` to exist, so the edge is not merely acyclic but genuinely one-way. The only cost is schedule: T-006 moves onto the critical path behind T-007's roughly six-minute part-1 run. The rationale is written into T-006 rather than left in the dep list, which is what will keep a future reviser from "optimising" the edge away. One small inaccuracy in that passage: T-006 justifies its one-case pin by saying "T-009 verifies the assembled suite and one case is the number it will expect", but T-009 explicitly does not pin it — its floors cover the seven pre-existing files "plus whatever `tests/lint.bats` adds". The pin is a fine choice; only its stated reason is off. | T-006.md:20 (deps) and T-006.md:37 (the ordering rationale, carrying both directions of the argument). T-006.md:51 (`one case is the number it will expect`) against T-009.md:50 (`plus whatever tests/lint.bats adds`). Measured: `/usr/bin/grep -nE '^[[:space:]]*(\\[\\[\|\\(\\()' tests/*.bats tests/helpers.bash tests/mutation-check.sh` with the last file absent -> exit 2, no stdout. |
| coverage | info | The concurrent dispatch was the right call and nothing needs holding. Every finding this round lands in T-008 and T-009, both still `pending` and two waves out; T-002 through T-005 are at `checking` and none of them is touched by anything above. Their fixes verified: all four conversion tables now label their numbers as baseline `61213fe` and **now +2 in the working tree**, and all four tell the worker to locate sites by text rather than by number, which is the instruction that actually survives the next line shift. T-009 gained B-1 over the whole surface in one sweep with the reason recorded in the check itself — that `tests/install-failures.bats` is covered nowhere else — which is the seam closed rather than merely papered. The dep graph is acyclic with the new edge, all nine clauses are still claimed across the nine tasks, and the wave structure is T-001 -> T-002..T-005 -> T-007 -> T-006 -> T-008 -> T-009 as you describe. | Statuses: T-001 complete; T-002..T-005 checking; T-006..T-009 pending. Deps: T-001 []; T-002..T-005 [T-001]; T-007 [T-002..T-005]; T-006 [T-002..T-005, T-007]; T-008 [T-006, T-007]; T-009 [T-008]. Union of `clauses:` across all nine -> B-1 B-2 B-3 D-1 D-2 D-3 V-1 V-2 V-3. T-003.md:37, T-004.md:37, T-005.md:35 for the corrected table headers; T-009.md's check_method for the B-1 sweep. |
| coverage | info | The courier crossing on T-001 is worth keeping in the record for what it says about the check, not just about the hole. Two independent instruments reached the same `install-failures.bats` gap from opposite directions — the far side saw B-1's full surface and could not see the task's scoped `check_method`, so it read the evidence as insufficient; this audit saw both and read it as a seam between task boundaries. Neither would have found it alone from the other's vantage. That is the case for the dual-check regime stated more cleanly than an argument could, and it is the kind of thing the #34 evaluation should have on file. | T-001.md `## Courier comparison` section, against this audit's DEC-audit-r0 finding on B-1's per-file split. Measured basis shared by both: no task's `check_method` named tests/install-failures.bats in a first-token `[[` context before the fix; T-009 now does. |
