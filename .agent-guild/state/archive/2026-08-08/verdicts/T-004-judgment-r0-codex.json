{"task_id":"T-004","checker":"checker-courier","vendor":"openai","model":"gpt-5.6-terra","verdict":"pass","findings":[{"clause_id":"B-2","severity":"blocker","description":"Conversion 1 preserves the byte-identical needle `bundles = [\"media\"]`, passes the unchanged `$(registry_line 'owner/repo/x')` haystack as argument two, uses literal-equivalent fixed-string containment, and calls `assert_contains` directly.","evidence":"Self-contained evidence: conversion 1, lines 135→137."},{"clause_id":"B-2","severity":"blocker","description":"Conversion 2 preserves the byte-identical needle `[\"cloud\"]`, passes the unchanged `$(registry_line spotify)` haystack as argument two, uses literal-equivalent fixed-string containment, and calls `assert_contains` directly.","evidence":"Self-contained evidence: conversion 2, lines 142→144."},{"clause_id":"B-2","severity":"blocker","description":"Conversion 3 preserves the byte-identical needle `[\"cloud\"]`, passes the unchanged `$(registry_line 1password)` haystack as argument two, uses literal-equivalent fixed-string containment, and calls `assert_contains` directly.","evidence":"Self-contained evidence: conversion 3, lines 143→145."},{"clause_id":"B-2","severity":"blocker","description":"Conversion 4 preserves the byte-identical needle `bundles = [\"core\", \"media\"]`, passes the unchanged `$(registry_line spotify)` haystack as argument two, uses literal-equivalent fixed-string containment, and calls `assert_contains` directly.","evidence":"Self-contained evidence: conversion 4, lines 150→152."},{"clause_id":"B-2","severity":"blocker","description":"Conversion 5 preserves the byte-identical needle `{ name = \"yq\", type = \"brew\", bundles = [\"core\"] },`, including its load-bearing trailing comma, passes the unchanged `$(registry_line yq)` haystack as argument two, uses literal-equivalent fixed-string containment, and calls `assert_contains` directly.","evidence":"Self-contained evidence: conversion 5, lines 190→192."},{"clause_id":"B-2","severity":"blocker","description":"Conversion 6 preserves the byte-identical needle `found 0`, retains `$output` through the helper default haystack, uses literal-equivalent fixed-string containment, and calls `assert_contains` directly.","evidence":"Self-contained evidence: conversion 6, lines 179→181; helper default `${2-$output}`."},{"clause_id":"B-2","severity":"blocker","description":"Conversion 7 preserves the byte-identical needle `nothing untracked`, retains `$output` through the helper default haystack, uses literal-equivalent fixed-string containment, and calls `assert_contains` directly.","evidence":"Self-contained evidence: conversion 7, lines 202→204; helper default `${2-$output}`."},{"clause_id":"B-2","severity":"blocker","description":"Conversion 8 preserves the byte-identical needle `fzf`, retains `$output` through the helper default haystack, uses literal-equivalent fixed-string containment, and calls `assert_contains` directly.","evidence":"Self-contained evidence: conversion 8, lines 274→276; helper default `${2-$output}`."},{"clause_id":"B-2","severity":"blocker","description":"Conversion 9 preserves the byte-identical needle `unknown option`, retains `$output` through the helper default haystack, uses literal-equivalent fixed-string containment, and calls `assert_contains` directly.","evidence":"Self-contained evidence: conversion 9, lines 280→282; helper default `${2-$output}`."},{"clause_id":"B-2","severity":"blocker","description":"Conversion 10 preserves the byte-identical needle `--adopt`, retains `$output` through the helper default haystack, uses literal-equivalent fixed-string containment, and the helper's `--` guard preserves this leading-dash needle while calling `assert_contains` directly.","evidence":"Self-contained evidence: conversion 10, lines 286→288; helper `grep -qF -- \"$needle\"`."},{"clause_id":"B-2","severity":"blocker","description":"Conversion 11 preserves the byte-identical needle `--machine`, retains `$output` through the helper default haystack, uses literal-equivalent fixed-string containment, and the helper's `--` guard preserves this leading-dash needle while calling `assert_contains` directly.","evidence":"Self-contained evidence: conversion 11, lines 287→289; helper `grep -qF -- \"$needle\"`."},{"clause_id":"B-2","severity":"blocker","description":"All 11 conversions pass: each retains its needle and haystack, preserves positive containment semantics because every quoted baseline needle is literal and `grep -qF` performs literal matching, retains the yq comma, and routes the assertion through the shared helper.","evidence":"Self-contained baseline-to-current evidence for conversions 1–11 and supplied `assert_contains` implementation."}],"timestamp":"2026-08-08T00:00:00Z","duration_ms":null,"cost_usd":null}