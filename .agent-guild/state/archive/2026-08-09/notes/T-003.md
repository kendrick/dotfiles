# T-003 self-report

## What I did

Committed T-001's guard and the sync suite (T-002's four cases plus T-004's fifth) as two commits on `main`, then verified D-1, D-2 and D-3 against `298c8a6..HEAD`. Nothing pushed.

- `d2a02f6698ccfea89eb4e61a41de79dc5b286345` — `fix(sync): refuse to commit from a source dir that can't be pushed`
- `faba26fed35566084408fdc98c7abff1f6e8b800` — `test: prove the sync guard fires in each blocked state, and on a clean tree`

Pre-flight: `git log --oneline 298c8a6..HEAD` was empty and `HEAD` was `298c8a6` on `main`, so the auto-sync LaunchAgent had left nothing behind. Working tree held exactly the two expected paths. Script hashed to `c90191bf…dc73` at the moment of staging, checked in the same command as the `git add`.

## Taste calls

**Two commits, not three, and not one.** Guard first, tests second. Tests-first is red at its own checkout, so that order was never available. The real choice was two versus three: T-004 is a separate task, so a separate commit for its single case was on the table. I merged it into the test commit. A third commit adding 18 lines to a file introduced one commit earlier, with nothing in between, is a fixup in history terms — it records the guild's internal task boundary rather than a unit of work a reader would want isolated. Both cases test the same guard through the same harness. The clean-tree case's distinct rationale survives as its own paragraph in the test commit's body, which is where a reader looks for it anyway.

I kept the guard and its tests apart rather than combining them (the constitution permits either) because they answer different questions. Someone bisecting a behavior change wants the behavior commit alone, and the two have genuinely different stories: one is the incident, one is why the harness looks so elaborate.

**Message content.** Both were run through the `humanizer` skill per the user's standing instruction. The audit killed four things: a manufactured closer on msg1's incident paragraph, an aphorism opener on msg2 ("a suite that can't tell it apart from its absence"), the near-uniform paragraph lengths, and a `reason`/`reasons` echo. Voice calibration came from the last ~18 commit bodies rather than the skill's defaults — this repo writes long unwrapped paragraphs, opens on the concrete mechanism, and uses colons where the skill would reach for a dash. Em dashes: the sample runs about 4 across 40 bodies, so I kept exactly one construction, unspaced, in msg1's precedence paragraph.

I verified every line reference before writing it rather than copying from the constitution: `:66` is the unguarded `chezmoi re-add` (it exits 1 inside `if !`, taking the run down before the guard), `:142` folds `date -u` into the commit subject, `:128`/`:130` bracket the guard's placement. I also checked the LaunchAgent plist before describing its cadence — an early draft said "every fifteen minutes"; it is actually a twice-daily `StartCalendarInterval`, so the message says "unattended" and names no interval.

**Worktrees instead of a detached checkout for D-3.** The check method says to check out each sha. I ran each one in a throwaway `git worktree` under the scratchpad and removed both afterward. Same measurement, and it never detaches the real checkout — which, in a job about a six-week detached-HEAD incident, seemed like the wrong risk to take for convenience. `git worktree list` is back to one entry and `git symbolic-ref -q HEAD` reads `refs/heads/main`.

## Verification

D-1: name-only yields exactly `dot_local/bin/executable_dotfiles-sync` and `tests/sync.bats`; `check-diff-scope.py` exits 0; removed-line count on the script is 0.
D-2: `bats tests/` exits 0 with 0 `not ok` and 0 skips; `bats --count` is 125; floors hold exactly at 18/11/44/17/9/7/1/13 with sync contributing 5; removed-line count over `tests/` is 0; no added `^\+\s*skip\b`.
D-3: `d2a02f6` runs 120 green, `faba26f` runs 125 green.

Also re-ran B-1 in committed form, since this is the first time the diff has the shape the constitution actually specifies. Pattern confirmed live against B-1's own failing example first, then zero matches across 15 added non-comment lines. Added-line set non-empty, so the run is not void. D-4 by reading: all 22 added lines are the guard (8 comment, 13 control flow and messages, 1 `head_sha` assignment). Nothing touches the push classifier at `:159-182`, no new phase.

## Something pushed, and it was not me

After the verification finished I checked `origin/main` out of habit and found it at `faba26f`, my second commit. I ran no `git push`. Escalating rather than shrugging, because "do not push" was the task's hard constraint and the user's standing rule is that they push by hand.

What I can show:

- `git reflog show origin/main --date=iso` records one new entry, `faba26f … 2026-08-08 22:37:50 -0500: update by push`. One entry, not two, so a single push carried both commits after the second one existed.
- My commits are at 22:36:09 and 22:36:12. The push is 98 seconds later.
- `git ls-remote origin main` returns `faba26fed35566084408fdc98c7abff1f6e8b800`, so it is really on the remote, not a stale local ref.
- No `core.hooksPath` is set at repo or global scope, and `.git/hooks/` holds nothing but `.sample` files. No hook did this.
- The LaunchAgent did not. `launchctl list` shows nothing matching `dotfiles`, and `~/.local/state/dotfiles/last-sync.log` ends at the 22:00 run, which committed `75a2325` (`tests/sync.bats`, 181 insertions, an earlier version without T-004's fifth case) and then printed `Auto-push is off; push manually`. That is the collision the brief warned about; HEAD reflog shows it reset back to `298c8a6` at 22:05:31, before I started.
- The test suite cannot reach this repo's remote. Every case pushes only its own scratch repo to its own bare origin, and `DOTFILES_AUTO_PUSH` is unset, so the script's push phase never fires.

That rules out every automated route I can check. The remaining explanation is a person or another session pushing by hand in that 98-second window, and there are eight peer sessions open on this machine. I cannot distinguish between them from here, so I am reporting it rather than naming a cause.

Nothing about it changes the verification. D-1, D-2 and D-3 all resolve `298c8a6..HEAD` locally, and that range is untouched by a push.

## Findings about upstream tasks

None. Nothing needed patching, and I edited none of T-001's, T-002's or T-004's artifacts.

One observation worth passing on rather than acting on: the task brief said the guard was "22 added lines," and `git diff --stat` reports 23 insertions. Both are right. The 23rd is the blank line separating the guard from the clean-tree exit, which `grep -c '^+[^+]'` does not count. Not a discrepancy, just two different counts of the same change.
