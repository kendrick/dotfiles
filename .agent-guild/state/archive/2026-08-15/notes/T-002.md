# T-002 self-report

Resumed a stalled dispatch. The substantive rewording was already in the working tree; the outstanding step was the humanizer pass over the prose it added. Ran that, and it moved the text meaningfully, so both comment blocks and both headings changed again.

## What changed

One file: `dot_local/bin/executable_dotfiles-doctor`. `tests/doctor.bats` is untouched, because I kept the prefix `in a bundle you've enabled but not installed` intact and the assertion at `:99` matches on that prefix alone.

Headings now read:

- `:21` — `(install by hand; apply won't retry until the list changes)`
- `:68` — `(install by hand; apply won't retry until the package list changes)`

## Taste calls

**The humanizer's real catch was pattern 30, diff-anchored writing.** Both comments as I inherited them narrated the edit instead of the mechanism. The VS Code one opened `Not "next apply installs these", which is what this used to claim`, quoting a string sitting one line below it, and the Homebrew one closed on `it's why this heading no longer tells you to wait for the next apply`. Both are comments about a diff rather than about the code, and the second one paraphrases the very `echo` it precedes, which is what C-8's last sentence forbids. Rewrote both to lead with the hash model and end on the consequence, letting the `echo` carry the instruction. The comment explains why the list persists; the heading says what to do. Neither restates the other now.

**Dropped "with a sharper edge."** Manufactured punchline that named no actual difference. The house voice in this file states the distinction outright, the way `Against \`brew list\`, not \`brew leaves\`: a tracked formula that something else depends on isn't a leaf` does two lines up.

**Two smaller humanizer hits on my own revision.** I first ended the Homebrew block on `the name sits in this list run after run, unretried` — a tailing-negation fragment (pattern 9), replaced with `the same missing name in this list, run after run`. And `stays missing through every apply after` read truncated, so it became `no later apply goes back for the extension the marketplace pulled`.

**Em dashes: none in the added prose.** Worth flagging as a deliberate conflict resolution. The file's existing comments do use spaced em dashes (`installer.rb:84-87 returns on a fetch failure — so the install phase never runs` in the installer, and `none — every family...` in the output strings here), so matching voice would argue for them. But the user's standing preference bans the spaced form outright, and the unspaced form would clash with the file's own habit. Sidestepped both by writing around the dash entirely, using the colon-then-reason construction the file already leans on heavily.

**Tightened the headings toward house terseness.** The neighbors are `(uninstall, or capture with dotfiles-sync)`, `(add it to .chezmoidata.toml, or uninstall)`, `(edit the source template, or reconcile by hand)`. `install these by hand; apply won't try again` was wordier than any of them, so it lost `these` and `try again` became `retry`. `install by hand` deliberately echoes `reconcile by hand` at `:155`.

**Left the retry condition doing double duty.** `apply won't retry until the package list changes` states the retry condition and, implicitly, the other way out: edit the list. I considered spelling out `or drop it from .chezmoidata.toml` to mirror the two-option house pattern, but the heading was already the longest line in the file and C-7 reserves that file for a human anyway. The implicit pointer felt like the better trade.

**Rewrapped to the file's width.** The inherited Homebrew block ran to 105 chars against the file's 91-96 for comment prose. Both blocks now sit at or under 96. This is a hard-wrap inside a code comment, not prose, so the never-hard-wrap preference doesn't apply; the file's own convention governs.

## Fact-checks against C-6's "no behavior the script does not have"

Every claim in the new comments was re-derived from source, not assumed:

- `keys the extension installer on this file's hash` — `run_onchange_install-vscode-extensions.sh.tmpl:3` hashes `dot_config/vscode-extensions.txt`, which is the same path `$list` points at here.
- `it names the id and exits 0` — that script collects rejected ids into `failed`, prints them, and closes on a bare `exit 0` under the comment `Named the failures and still succeeded, on purpose`.
- `keys on the rendered Brewfile` — `run_onchange_install-packages.sh.tmpl:2`, `includeTemplate "Brewfile" . | sha256sum`.
- `exits 0 by design (#17)` — corroborated by the constitution's non-goals.

Also grepped the whole file for surviving retry promises. The only hit for `will install` is pre-existing fonts prose at `:101` (`configs naming families nothing will install`), which is a gap report, not a retry claim.

## Verification

- `bash -n dot_local/bin/executable_dotfiles-doctor` — clean.
- `bats tests/doctor.bats` — 11/11.
- `git diff --name-only` confirms my only write is the doctor. The other entries (`run_onchange_install-packages.sh.tmpl`, `tests/install-failures.bats`) are T-001 running concurrently, and `dot_claude/encrypted_private_settings.json.age` was already dirty at session start.

No protected-passages manifest to check; the constitution declares none.
