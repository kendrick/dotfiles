---
task: CON-audit
checker: auditor
vendor: anthropic
model: claude-opus-5[1m]
verdict: PASS
checked_at: 2026-08-14T22:35:55Z
---

## Scope of this round

Constitution digest at audit `9963d105c63a9cb79890ee516e7c4311031cea9af1eff11dace6578c6dde7d1f`, matching `CON-audit-r1.md.sha256`. Every runnable check was executed against apparatus built this round, not read, and nothing was inherited from round 0 — the six clauses round 0 passed were re-derived from scratch, which is how this round found the C-4 miscount round 0 missed.

A reference implementation of the deliverable was built from the clause text alone at `.agent-guild/state/apparatus/CON-audit-r1/ref/` (fixed installer plus the four cases C-1, C-2 and C-3 require; 9/9 green), with ten counter-variants beside it: `v1-unfixed-installer`, `v2-attack-doublerun`, `v4-prevalidate`, `v5-unbounded`, `v6-c4-regression`, `v7-lint-canary`, `v8-c4-existing`, `v9-count-one`, `v10-count-three`, `v11-shared-log`, `v12-summary-only`. C-7's git-state property was exercised in a throwaway repo under `mktemp -d`, never under this tree, and the repo was removed after. No `.chezmoi*` file was created anywhere under the source dir; `chezmoi execute-template '{{ template "bundles" . }}'` printed the bundle list before the first variant was built and again after the last, so no check in this round ran against a poisoned renderer. `git status --porcelain` reads what it read at start (` M dot_claude/encrypted_private_settings.json.age`); nothing moved, nothing was restored.

Weight line present, not a placeholder, reason intact, and the derivation still holds after the revision: the bats harness exists and needs extending, and `run_onchange` fires unattended during apply. Eight clauses, exactly at the standard ceiling — the revision added rubric text, not clauses. No contradictions introduced: C-5's new condition (c) quotes C-1, C-2 and C-3 back accurately in every particular, and C-1's second `brew bundle` call still sits behind a precondition C-2's single-call requirement cannot reach. Protected content is `none`, no manifest to parse.

Baseline before any variant: `bats tests/` 125/125, exit 0. C-1, C-2 and C-3 all red on the untouched tree, as they should be with the work not yet done.

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-1 | blocker | PASS — round 0's count half is closed and re-derived independently. The gate discriminates three ways: exit 0 against the reference's two honest cases, exit 1 at one case, exit 1 at three. `bats -c` returns a clean `0` with RC 0 when the filter matches nothing, so the baseline red is `[ 0 -eq 2 ]` failing rather than a crash at a precondition. Red against the clause's own failing example — an honest suite against the installer as it stands today fails on `[ "$(count_calls bundle)" -eq 2 ]`, which is the assertion C-1's text names. Round 0's assertion-content half is closed by C-5(c), not here; see the diagnosis-free note below. | ref/ C1_EXIT=0; v1-unfixed-installer C1_EXIT=1 on `count_calls bundle`; v9-count-one exit 1; v10-count-three exit 1; `bats -c` = 0/2/1/3 measured |
| C-2 | blocker | PASS — filter selects exactly the case described. Red on the untouched tree (zero matches → `bats` "Found no tests", exit 1) and red against `v4-prevalidate`, the clause's own failing example, failing on `[ "$(count_calls info)" -eq 0 ]` with the up-front `brew info` sweep planted ahead of the bundle call. Green against the reference. C-5(c) now names both counts, matching this clause's text word for word. | ref/ C2_EXIT=0; v4-prevalidate exit 1 on `count_calls info`; baseline exit 1 |
| C-3 | blocker | PASS — red against `v5-unbounded`, the clause's own failing example, where the retry loops on each new failure: fails on `[ "$(count_calls bundle)" -eq 2 ]`. Also red against `v1-unfixed-installer` on the same assertion, since a script with no retry never reaches two. Green against the reference. | ref/ C3_EXIT=0; v5-unbounded exit 1; v1-unfixed-installer C3_EXIT=1 |
| C-4 | blocker | PASS on the check, with one factual correction to the clause's prose. Green end to end at baseline (125 cases, exit 0). Red against its own stated failing example: `v8-c4-existing` plants an unguarded empty-array expansion under `set -u` in the fetch-phase path and `install-packages: a fetch failure where every entry exists names the whole batch` — the case C-4's failing example names — goes red. The lint half discriminates too: a `[[ ]]` count assertion planted in a copy of `tests/` is caught by name and line. Correction: the clause says "the four existing `install-packages` cases at `tests/install-failures.bats:74-181`", but that range holds five (`:74`, `:81`, `:107`, `:141`, `:164`). The range is right, the count is off by one. Nothing turns on it — the check runs the whole suite either way. | baseline 125/125; v8-c4-existing case 4 red; v7-lint-canary lint.bats red naming `install-failures.bats:403`; `grep -c '^@test "install-packages'` = 5 |
| C-5 | blocker | PASS — condition (c) closes round 0's finding, verified against round 0's own artifact. `v12-summary-only` reconstructs the plausible-on-review pair (status 0 plus the indented name, no log) and it still passes C-1's deterministic check with exit 0 against the unfixed installer, so C-5(c) is the only thing standing between that suite and a green job. It stands: a summary-only case reads no `brew bundle` log at all, which (c) fails without ambiguity. The three conditions are falsifiable and each names an artifact I can describe. Judged by reading; nothing to execute. Two wording notes below, neither a fault. | v12-summary-only C1_EXIT=0 (so C-5 is load-bearing); rubric read against C-1/C-2/C-3 texts, quoted accurately in all three |
| C-6 | major | PASS — unchanged by this revision and re-derived rather than inherited. Both `next apply installs these` parentheticals exist where the clause says (`executable_dotfiles-doctor:17`, `:58`) and are still the only two occurrences in the file. Three FAIL conditions, each objective, anchored to the `run_onchange` hash comment at `run_onchange_install-packages.sh.tmpl:2-5`, which is present and says what the clause claims. Judged by reading. | dot_local/bin/executable_dotfiles-doctor:17,58; run_onchange_install-packages.sh.tmpl:2-5 |
| C-7 | blocker | PASS — discriminates in all four directions, re-derived in a fresh throwaway repo. Green on the job done right (six in-scope paths, the five the clause lists plus the ignored pre-existing dirty file), red on its own failing example with `.chezmoidata.toml` named on stderr, red on an untracked stray, green on a new file created under the allowed `_working-memory/` prefix. Run against the real tree it reports `OK: 1 path(s) in scope`, so the `--ignore` covers the one dirty path and the apparatus under `state/` is correctly invisible to it. | mktemp throwaway repo: V1 exit 0, V2 exit 1 (`out of scope: .chezmoidata.toml`), V3 exit 1, V4 exit 0; real tree exit 0 |
| C-8 | minor | PASS — unchanged and re-derived. The second FAIL condition is objective: the drop-and-retry branch and the no-third-attempt stop must each carry a why-comment, and both are places the deliverable must create, so the condition is checkable against the diff. The voice anchor resolves — `run_onchange_install-packages.sh.tmpl:30-35` is the "guard this replaces" incident note and `:62-67` the fetch-phase explanation, both substantive why-comments rather than restatements. Judged by reading. | run_onchange_install-packages.sh.tmpl:30-35, :62-67 |

## What this PASS certifies, and what it does not

It certifies that every clause discriminates against the failing example it states, executed rather than read, and that no artifact a worker acting in good faith can produce satisfies the checks while leaving #22 unfixed. Both round 0 findings are closed: the count half by C-1's gate, the assertion-content half by C-5(c), and I confirmed the second by re-running round 0's own `gapc` construction against the revised text.

It does not certify that a worker who abandons the implementation and forges the instrument will be caught by a script. One construction defeats C-1's deterministic check while carrying, literally, every assertion C-1's text names — recorded below, because you asked to know exactly how if it existed.

## Notes that are not findings

- **The one construction that still gets through, and why it is not a blocker.** `v2-attack-doublerun` holds two `drop-and-retry` cases that read a recorded log of every `brew bundle` invocation and the Brewfile each received, assert the log holds two invocations, assert the second Brewfile carries both survivors and not the dropped name, and are green against the installer as it stands today with no retry branch. C-1's check exits 0 on it. Two moves buy it: the dropped name is a fixture that was never in the rendered Brewfile, so "not the dropped name" is true of any Brewfile at all, and the case drives the installer twice, so the log accumulates two invocations without the script ever retrying. C-5's rubric does not reach it — (a) and (b) miss, and (c) is about omission, while this case omits nothing.

  What keeps it off the blocker list is that it cannot arrive by accident. Every rig of this family I could build is *also* red against a correct implementation: run the fixed installer twice under that stub and the log holds three invocations, not two. A worker who implements the retry and runs the check watches this test fail, so the only path to shipping it is to give up on the implementation first and then write a test asserting behavior the code does not have. `v11-shared-log` — the more plausible version, where the two cases share a `BATS_SUITE_TMPDIR` log instead of a per-test one — is self-defeating in the same way and simply goes red (the stub's call-number branch stops firing), so the plausible mistake fails loudly rather than passing quietly.

  One sentence closes it if you want it closed, in the clause's own idiom: add to C-5's (c) "or drives the installer more than once in a single case", and to C-1's first bullet that the name chosen to fail `brew info` must be an entry the rendered Brewfile actually contains. The second half matters on its own — with a fixture name the "not the dropped name" assertion is vacuous even in an honest suite, and my reference implementation only discovered the constraint because the survivor assertion is unsatisfiable without it.

- **C-1 leaves the source of the three names undetermined, and it resolves itself.** The clause says "a batch of three names where one fails `brew info`" without saying they must come from the render. The existing fetch-phase case at `:107` uses invented `fixture-*` names, so that is the convention a worker will reach for first — and it makes C-1's own Brewfile assertion unsatisfiable, since the retry drops lines from the rendered Brewfile and a fixture name was never in it. A worker discovers this on the first run. Undetermined, but not ambiguous in a way that lets a wrong implementation through.

- **C-5(c)'s "and their Brewfiles" over-reaches C-1's second bullet.** C-1's all-unresolvable case has one Brewfile and its named assertion is the invocation count, not a Brewfile's contents. A checker reading (c) literally could fail a correct second case for not asserting on a Brewfile that should not exist. "Its own clause" resolves it — the second bullet names only the one-invocation assertion — but the gloss reads stricter than the clause it glosses, and an over-strict FAIL here costs a dispute.

- **The `[[ ]]` vacuity vector is real and already closed, by C-4.** A count assertion written `[[ "$(count_calls bundle)" -eq 2 ]]` instead of `[ ... ]` does not gate anything on this machine — measured directly, `/bin/bash` 3.2.57, probe D passes while probes A, B, C and E all fail. A case carrying that assertion would satisfy C-5(c) on a reading and verify nothing. `tests/lint.bats` catches it: its guarded surface is `"$BATS_TEST_DIRNAME"/*.bats`, which includes `install-failures.bats`, and a planted canary was flagged by file and line. C-4 names that guard explicitly, so the constitution covers this without another clause.

- **Coverage still complete after the revision.** AC1 and AC2 map to C-1, AC3 to C-2, AC4 to C-3, AC5 to C-6, AC6 to C-4 and C-5; the registry non-goal maps to C-7. The revision added no bar the spec does not ask for — C-1's log requirement is the spec's own "measurable by stub invocation count" applied to the drop-and-retry path.
