---
task: CON-audit
checker: auditor
vendor: anthropic
model: claude-opus-5[1m]
verdict: PASS
checked_at: 2026-08-14T18:05:00Z
---

## Scope of this round

Constitution digest at audit `082e3c70a03fdcc2a55a4ea9fd1afb8276e2cbee8c2a950fbe36307bab958176`, matching `CON-audit-r2.md.sha256`. Round 2 exists because the four corrections round 1 volunteered were applied, re-closing the digest gate. Nothing was inherited from round 1: the reference implementation was rebuilt from the current clause text at `.agent-guild/state/apparatus/CON-audit-r2/ref/`, and every runnable check was executed against it rather than read.

Apparatus this round: `ref/` (fixed installer plus the four cases C-1, C-2 and C-3 require — 4/4 green filtered, 129/129 on the full suite), and the variants `v1-unfixed`, `v2a-doublerun-fixture`, `v2b-doublerun-realname`, `v4-prevalidate`, `v5-unbounded`, `v12-summary-only`, `v13-sharedlog-unfixed`, `v13-sharedlog-fixed`. C-4's whole-suite variants ran in a full repo copy under `mktemp -d`, and C-7's git-state property in a throwaway `git init` repo under `mktemp -d`; both were removed after. No `.chezmoi*` file was created anywhere under the source dir — `find .agent-guild/state/apparatus -name '.chezmoi*'` returns 0 — and the environment constraint was honored end to end: `chezmoi execute-template '{{ template "bundles" . }}'` printed the nine-bundle list before the first variant and again after the last, both RC 0. `git status --porcelain` reads what it read at start (` M dot_claude/encrypted_private_settings.json.age`); nothing moved, nothing was restored.

Weight line present, not a placeholder, reason intact, and the derivation still holds after the revision: the bats harness exists and needs extending, and `run_onchange` fires unattended during apply. Eight clauses, exactly at the standard ceiling — the four edits changed rubric and prose, not clause count. Protected content is `none`, no manifest to parse. No contradiction introduced by any edit: C-5(d) forbids a second *drive of the installer* inside one case, while C-1's mixed-batch case needs two *`brew bundle` invocations* from a single drive, and the reference demonstrates those are different things (one drive, log holds two `bundle` entries, green).

Baselines on the untouched tree: `bats tests/` 125/125, exit 0 — C-4's stated count is correct as written. C-1, C-2 and C-3 all red, as they should be with the work not yet done.

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-1 | blocker | PASS — edit 2 landed and does more than de-vacuate the assertion: it kills round 1's double-run attack outright, without needing the rubric. Re-derived from scratch. The gate discriminates four ways: green on the reference, red on the unfixed installer at `[ "$(count_calls bundle)" -eq 2 ]`, red at one case, red at three. Baseline red is a real assertion failure (`bats -c` returns `0` with RC 0, so `[ 0 -eq 2 ]` fails) rather than a crash at a precondition. Edit 2 verified against a compliant build of the attack: with the dropped name read out of the render, `v2b-doublerun-realname` dies at `! grep -qx "brew \"$dead\"" bundle-2`, because the unfixed installer hands the retry the full render, which contains that name. | ref C1_EXIT=0; v1-unfixed C1_EXIT=1 on `count_calls bundle`; v2b-doublerun-realname C1_EXIT=1 on the survivor assertion at `install-failures.bats:454`; baseline C1_EXIT=1 with `bats -c` = 0 |
| C-2 | blocker | PASS — unchanged by the edits and re-derived, not inherited. Filter selects exactly the case described. Red on the untouched tree (zero matches → `bats` "Found no tests", exit 1) and red against `v4-prevalidate`, the clause's own failing example, failing on `[ "$(count_calls info)" -eq 0 ]` with an up-front `brew info` sweep planted ahead of the bundle call. Green on the reference. C-5(c) still names both counts, matching this clause's text word for word. | ref C2_EXIT=0; v4-prevalidate exit 1 on `count_calls info` at `install-failures.bats:505`; baseline exit 1 |
| C-3 | blocker | PASS — unchanged and re-derived. Red against `v5-unbounded`, the clause's own failing example, where the retry loops on each new failure until one succeeds: fails on `[ "$(count_calls bundle)" -eq 2 ]`. Also red against `v1-unfixed` on the same assertion, since a script with no retry never reaches two. Green on the reference, which reports the second failure and stops at exactly two invocations while still exiting 0. | ref C3_EXIT=0; v5-unbounded exit 1 at `install-failures.bats:521`; v1-unfixed C3_EXIT=1 |
| C-4 | blocker | PASS, and edit 4's correction is itself correct on both counts. The five names the clause now lists match the five `@test` descriptions in the file exactly, `grep -c '^@test "install-packages'` returns 5, and `74-181` is the precise span (`:74`, `:81`, `:107`, `:141`, `:164`, closing at `:181`). The 125 figure is confirmed independently: `bats tests/` on the clean tree is 125 ok, 0 not ok, exit 0. Discriminates on both halves. Whole-suite half: with the reference deliverable installed the suite is 129/129 green, and planting the clause's own failing example — an empty array expanded without a length guard under `set -u` in the fetch-phase path — turns exactly `install-packages: a fetch failure where every entry exists names the whole batch` red, 128 ok / 1 not ok, exit 1. Lint half: a `[[ ]]` count assertion planted in `install-failures.bats` is caught by `no bare [[ or (( on the guarded surface`, named by file and line. | clean tree 125/125 exit 0; copy+ref 129/129 exit 0; C4_VARIANT_EXIT=1, `not ok 77`; lint exit 1 naming `install-failures.bats:463` |
| C-5 | blocker | PASS — both new conditions do the work they were added for, and the loosening did not reopen what (c) was written to close. (d) catches `v2a-doublerun-fixture` on a literal reading with no charity required: the case body carries two `run bash "$script"` lines, which is "drives the installer more than once within a single case" verbatim, and that construction still exits 0 on C-1's script check, so (d) is the only thing holding it. (c) after the loosening still fails `v12-summary-only` — the parenthetical scopes only the Brewfile requirement, so the log requirement continues to bind both `drop-and-retry` cases, and both of v12's cases read no log at all. The loosening also does what it was meant to: the reference's all-unresolvable case asserts the invocation count and the reported names, no Brewfile, and passes (c) as now written. Judged by reading, with the deterministic half of each construction executed. | v2a-doublerun-fixture C1_EXIT=0 (so (d) is load-bearing); v12-summary-only C1_EXIT=0 against the unfixed installer (so (c) is load-bearing); ref all-unresolvable case green and (c)-clean |
| C-6 | major | PASS — untouched by this revision and re-derived rather than inherited. Both `next apply installs these` parentheticals are still at `executable_dotfiles-doctor:17` and `:58` and are still the file's only two occurrences. Three FAIL conditions, each objective, anchored to the `run_onchange` hash comment at `run_onchange_install-packages.sh.tmpl:2-5`, which is present and says what the clause claims. Judged by reading; nothing to execute. | dot_local/bin/executable_dotfiles-doctor:17,58; run_onchange_install-packages.sh.tmpl:2-5 |
| C-7 | blocker | PASS — re-derived in a fresh throwaway repo under `mktemp -d`, discriminating in all four directions. Green on the job done right (`OK: 5 path(s) in scope`), red on its own failing example with `.chezmoidata.toml` named on stderr, red on an untracked stray, green on a new file created under the allowed `_working-memory/` prefix. Against the real tree it reports `OK: 1 path(s) in scope`, so the `--ignore` covers the one pre-existing dirty path and this round's apparatus under `state/` is correctly invisible to it. | throwaway repo: V1 exit 0, V2 exit 1 (`out of scope: .chezmoidata.toml`), V3 exit 1 (`stray.txt`), V4 exit 0; real tree exit 0 |
| C-8 | minor | PASS — untouched and re-derived. The second FAIL condition is objective: the drop-and-retry branch and the no-third-attempt stop must each carry a why-comment, and both are places the deliverable has to create, so the condition is checkable against the diff. The voice anchors resolve — `:30-35` is the guard-this-replaces incident note and `:62-67` the fetch-phase explanation, both substantive why-comments rather than restatements. Judged by reading. | run_onchange_install-packages.sh.tmpl:30-35, :62-67 |

## What this PASS certifies, and what it does not

It certifies that all four edits landed as intended, that each closes the specific construction it was aimed at, and that none of them broke a clause that was already sound. Every clause is red against its own stated failing example, executed rather than read, and the clause texts now state facts I measured directly: five `install-packages` cases spanning `:74-181`, and 125 cases green on the clean tree.

It does not certify that a worker who abandons the implementation and then forges the instrument will be caught by a script. One construction survives, in the same class round 1 documented and you accepted — recorded below rather than filed as a finding, because it is red against a correct implementation and so cannot arrive by accident.

## Notes that are not findings

- **Edit 2 is stronger than it looks; it does not need (d) to stop the double run.** I built the attack twice. `v2a-doublerun-fixture` is round 1's construction verbatim, dropped name a fixture the render never held, and it still exits 0 on C-1's script check — (d) is what stops it, and it does so on a literal reading. `v2b-doublerun-realname` is the same rig made edit-2-compliant, and it never reaches the rubric: the unfixed installer hands the retry the unmodified render, that render contains the dropped name because edit 2 requires it to, and the case dies at `! grep -qx "brew \"$dead\"" bundle-2`. So the two edits are independent stops on the same attack rather than one stop stated twice.

- **The loosening of (c) is safe, and the grammar carries it.** "does not read a recorded log of `brew bundle` invocations (and, for the mixed-batch case only, the Brewfile the second invocation received)" reads as two objects of *read*, with the scope restriction attached to the second. The log requirement therefore still binds both `drop-and-retry` cases, which is the limb `v12-summary-only` fails on. It is also independently anchored: (c)'s opening, "omits the assertion its own clause names", routes the all-unresolvable case straight to C-1's second bullet, which names the one-invocation assertion. Re-derived rather than reasoned from round 1 — I rebuilt v12 and confirmed it is still green on C-1's deterministic check against the unfixed installer, so C-5 remains the only thing between that suite and a green job.

- **The route around edit 2 that remains, and why it stays a note.** `v13-sharedlog-unfixed` satisfies edit 2 (all three names read out of the render), evades (d) (each case drives the installer exactly once), satisfies (c) literally (it reads a `brew bundle` log and the second invocation's Brewfile), and is green against the installer as it stands today. Two moves buy it: the invocation log lives in `BATS_SUITE_TMPDIR` so it accumulates across the two cases rather than within one, and the negative assertion's grep pattern carries a trailing space that can never match. It is green in exactly the same way round 1's v2 was — and self-defeating in exactly the same way. `v13-sharedlog-fixed` is the identical suite against the correct implementation: the shared log reaches three invocations and the case goes red at `[ "$(count_calls bundle)" -eq 2 ]`. A worker who does the work watches this test fail, so shipping it means giving up on the implementation first and then writing a test for behavior the code does not have. If you ever want it closed, the idiom is one more limb on (c): the invocation log must be per-case, and the survivor and dropped-name patterns must be exact matches on the recorded Brewfile.

- **(d) has no false-positive surface in this job.** None of the cases C-1, C-2 or C-3 require has any reason to drive the installer twice, so a correct suite never trips (d) and no dispute is waiting there.

- **Coverage still complete after the revision.** AC1 and AC2 map to C-1, AC3 to C-2, AC4 to C-3, AC5 to C-6, AC6 to C-4 and C-5; the registry non-goal maps to C-7. The edits added no bar the spec does not already ask for — edit 2 is the spec's own "measurable by stub invocation count" discipline applied to the one assertion where an invented name would have made it unmeasurable.
