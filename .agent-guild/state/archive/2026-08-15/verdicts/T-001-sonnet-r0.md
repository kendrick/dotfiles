---
task: T-001
checker: checker-judgment
vendor: anthropic
model: claude-opus-5[1m]
verdict: PASS
checked_at: 2026-08-15T05:25:51Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-1 | info | The clause's script check passes: exactly two cases carry `drop-and-retry` and both are green, and both prove their claim from a recorded log of every `brew bundle` invocation rather than from summary text. | $ .agent-guild/scripts/check-build.sh '[ "$(bats -c -f "drop-and-retry" tests/install-failures.bats)" -eq 2 ] && bats -f "drop-and-retry" tests/install-failures.bats' 1..2 ok 1 install-packages: drop-and-retry, mixed batch installs the survivors ok 2 install-packages: drop-and-retry, all unresolvable reports without retrying check-build.sh: exit 0 (log: .agent-guild/state/log/build-20260815T002147.log) Assertions re-read at tests/install-failures.bats:238-242 (log count == 2; second Brewfile carries both survivors, not the dropped name) and :277 (log count == 1). |
| C-1 | info | The retry Brewfile the second `brew bundle` received was dumped directly: it is the full render minus exactly the one dropped line, with both survivors present. | Instrumented copy of the suite under mktemp -d, dumping $BUNDLE_DIR/1 and $BUNDLE_DIR/2 from the mixed-batch case: entry lines in 1st Brewfile: 90 entry lines in 2nd Brewfile: 89 2nd contains: brew "age" / brew "chezmoi" occurrences of 'bats-core' in 2nd Brewfile: 0 DROP_NAME resolved to bats-core, SURVIVOR1=age, SURVIVOR2=chezmoi. |
| C-1 | info | The three names are read out of the rendered Brewfile rather than invented, so dropping one is observable and "not the dropped name" is not vacuously true. | tests/install-failures.bats:52-54 (render_brewfile) and :202-205, :252-255, :315-318 (names=($(printf '%s\\n' "$brewfile" \| grep '^brew "' \| head -3 \| sed -E 's/^brew "([^"]+)"$/\\1/'))). $ chezmoi execute-template <<<'{{ template "Brewfile" . }}' \| grep '^brew "' \| head -3 \| sed -E 's/^brew "([^"]+)"$/\\1/' age bats-core chezmoi These are real entries of the live render, not fixtures. |
| C-1 | info | The installer gates the retry on "not every batched name was unresolvable" (a count comparison) rather than on the trimmed Brewfile being non-empty, using `[ ]` rather than `[[ ]]` per the bash 3.2 rule. | run_onchange_install-packages.sh.tmpl:128 if [ "$allow_retry" -eq 1 ] && [ "${#missing_arr[@]}" -lt "${#fetch_arr[@]}" ]; then Mutant test: replacing that line with `if [ "$allow_retry" -eq 1 ]; then` (the "Brewfile still non-empty" gate the spec warns about) turns the all-unresolvable case RED: not ok 2 install-packages: drop-and-retry, all unresolvable reports without retrying `[ "$(wc -l <"$BUNDLE_LOG")" -eq 1 ]' failed (line 277) So that case discriminates the correct gate from the wrong one. |
| C-2 | info | The happy-path clause's script check passes; the case asserts both the `info` count is 0 and the `bundle` count is 1. | $ .agent-guild/scripts/check-build.sh 'bats -f "no extra brew calls" tests/install-failures.bats' 1..1 ok 1 install-packages: no extra brew calls on a clean run check-build.sh: exit 0 (log: .agent-guild/state/log/build-20260815T002151.log) tests/install-failures.bats:305-306 [ "$(wc -l <"$INFO_LOG")" -eq 0 ] [ "$(wc -l <"$BUNDLE_LOG")" -eq 1 ] |
| C-3 | info | The clause's script check passes; the case asserts the `bundle` count is exactly 2 and the script still exits 0, and it goes red against an installer with no retry branch. | $ .agent-guild/scripts/check-build.sh 'bats -f "reports and stops" tests/install-failures.bats' 1..1 ok 1 install-packages: reports and stops after a second failure check-build.sh: exit 0 (log: .agent-guild/state/log/build-20260815T002156.log) tests/install-failures.bats:349-350 [ "$status" -eq 0 ] [ "$(wc -l <"$BUNDLE_LOG")" -eq 2 ] Against HEAD's installer: not ok 1 ... `[ "$(wc -l <"$BUNDLE_LOG")" -eq 2 ]' failed (line 350). No third call is possible by construction: handle_bundle_result recurses once with allow_retry=0 (run_onchange_install-packages.sh.tmpl:144). |
| C-5 | info | The decisive test: rebuilt HEAD's installer into a scratch tree under mktemp -d with the new suite, and both retry-proving checks go RED there, so neither rests on summary text the unfixed installer already prints. | $ git show HEAD:run_onchange_install-packages.sh.tmpl > $SCRATCH/... (sha 3def466f6cc1f41ddb1ae2b94f3e136a7866d8bc; grep for 'retry\|allow_retry\|handle_bundle_result' returns nothing — no retry machinery in HEAD) $ bats -f "drop-and-retry" tests/install-failures.bats not ok 1 install-packages: drop-and-retry, mixed batch installs the survivors `[ "$(wc -l <"$BUNDLE_LOG")" -eq 2 ]' failed (line 238) ok 2 install-packages: drop-and-retry, all unresolvable reports without retrying $ bats -f "reports and stops" tests/install-failures.bats not ok 1 install-packages: reports and stops after a second failure `[ "$(wc -l <"$BUNDLE_LOG")" -eq 2 ]' failed (line 350) |
| C-5 | info | Rubric (a): every added case drives the rendered script as a real subprocess with brew stubbed on PATH, reaching no real Homebrew and mutating no machine state. | tests/install-failures.bats:24-28 (setup exports a synthetic HOME under $BATS_TEST_TMPDIR) and :236, :275, :303, :348 (PATH="$STUBS:/usr/bin:/bin" run bash "$script"). $ which brew /opt/homebrew/bin/brew $ PATH="/usr/bin:/bin" command -v brew (no output) -> real brew is unreachable on the test PATH; the stub is the only brew. The only real-$HOME use is chezmoi execute-template for rendering (tests/install-failures.bats:33-37, :52-54), which is read-only and predates this job. |
| C-5 | info | Rubric (b): no case proves its claim from a string the stub echoed — the mixed-batch case reads the Brewfile the script itself piped in, and the text assertions use the two-space indent only the script's own summary emits. | tests/install-failures.bats:238-242 asserts on $BUNDLE_LOG's line count and on $BUNDLE_DIR/2, which the stub captures via `cat >"$BUNDLE_DIR/$n"` (:224) from the script's own `brew bundle --file=/dev/stdin <<<"$retry_brewfile"` (run_onchange_install-packages.sh.tmpl:141). That content is the script's output, not the stub's. The supporting text assertions (:278-279, :351) match a two-space prefix produced only by `sed 's/^/ /'` at run_onchange_install-packages.sh.tmpl:126; the stubs emit names with a single leading space ("Fetching $NAME1, $NAME2"). Mutant test proving teeth: neutralizing the drop loop at run_onchange_install-packages.sh.tmpl:137 (retry still fires, dead line never dropped) turns the mixed-batch case RED at line 242 on `assert_not_contains "\\"$DROP_NAME\\"" "$second"`. |
| C-5 | info | Rubric (c): each added case carries the assertion its own clause names, and the invocation logs are per-case rather than shared across cases. | mixed batch -> log count and 2nd Brewfile (tests/install-failures.bats:238-242); all unresolvable -> log count == 1 (:277); no extra brew calls -> both info and bundle counts (:305-306); reports and stops -> bundle count == 2 (:350). $ grep -n "BATS_SUITE_TMPDIR" tests/install-failures.bats (no matches) All four logs are under $BATS_TEST_TMPDIR: :206, :207, :256, :284, :285, :319. |
| C-5 | info | Rubric (d): no case drives the installer more than once — the file has exactly one `run bash "$script"` per test. | $ grep -c 'run bash "$script"' tests/install-failures.bats 21 $ bats tests/install-failures.bats \| head -1 1..21 21 drive-lines across 21 cases: exactly one each. |
| C-5 | minor | One note for the orchestrator, not a defect: the `no extra brew calls` check is green against HEAD's installer, which the dispatch framed as an automatic C-5 failure, but C-2's own text defines that case as a baseline regression guard ("which is what the script does today"), so red at HEAD is impossible without violating C-2, and C-5's rubric (a)-(d) names no such condition. | constitution.md:16 — "A run in which `brew bundle` exits 0 makes no `brew info` call and exactly one `brew bundle` call, which is what the script does today." $ bats -f "no extra brew calls" tests/install-failures.bats # against HEAD's installer 1..1 ok 1 install-packages: no extra brew calls on a clean run The same holds for the all-unresolvable case, whose C-1-mandated assertion (log count == 1) HEAD also satisfies; its discriminating power is against a wrongly-gated retry, demonstrated by the mutant above. The two cases that can distinguish a fixed installer both go red at HEAD. |
| C-8 | info | The drop-and-retry branch carries an explanation of why it is gated on the count comparison rather than on a non-empty Brewfile — a constraint a reader would otherwise miss, not a paraphrase of the code. | run_onchange_install-packages.sh.tmpl:129-134 # At least one batched name resolves, so the registry isn't entirely to blame — drop # only the dead lines and give the survivors the run they never got, since # installer.rb's fetch phase returns before installing anything at all. Gating on the # count comparison (not on whether the trimmed Brewfile is merely non-empty) matters: # the fetch batch is a handful of entries out of dozens in the full Brewfile, so # "the Brewfile still has lines in it" stays true even when every batched name is dead. |
| C-8 | info | The no-third-attempt stop carries its own explanation, citing the issue that ruled out looping the apply. | run_onchange_install-packages.sh.tmpl:70-74 # Reports one completed `brew bundle` attempt and, the first time only, retries. $log is # the tee'd output, $status is brew's own exit code (PIPESTATUS, not tee's), and # $allow_retry is 1 for the initial attempt and 0 for the retry this function may itself # trigger — #17 already ruled out looping the apply, so a second failure of either phase # is always just reported, never retried again. |
| C-8 | info | The remaining added comments each explain a constraint or a surprising invariant rather than restating the adjacent line, matching the register of run_onchange_install-packages.sh.tmpl:30-35 and :62-67 (upstream line citations, issue numbers, incident dates). | run_onchange_install-packages.sh.tmpl:113-114 — "bash 3.2 renders an empty array as one empty word under set -u, so both expansions below are count-guarded rather than trusted to iterate zero times." (invariant, not a paraphrase) :152-155 — "$retry_log is unset unless handle_bundle_result actually retries; an unset trap reference just expands empty, so rm -f no-ops on the happy path instead of needing a second trap registration." (verified accurate: retry_log is assigned without `local` at :140, the trap is single-quoted at :157, and the script sets no `set -u`) :34-35 — "Factored out because #22's retry needs this same read applied to a second log, not just the first." (why the extraction happened) No added comment paraphrases its own code; I read every `+`-prefixed comment line in `git diff run_onchange_install-packages.sh.tmpl`. |
