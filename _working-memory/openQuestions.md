# Open Questions

<!-- Things that are unresolved and should not be guessed at. -->
<!-- Agents encountering these should ask rather than assume. -->

- Should this repo have its own lint/format gate (shellcheck / shfmt on the bash scripts, prettier on templates)? AGENTS.md's Build/Test/Lint section is intentionally empty — there's currently no CI or pre-commit on the dotfiles themselves. _(AGENTS.md)_
- The deployed `.eslintrc` / `.prettierrc` / `.stylelintrc` are global *user* config, not tooling for this repo — confirm they're never meant to run against the dotfiles source. _(dot_eslintrc, dot_prettierrc, dot_stylelintrc)_
- The `.chezmoiignore` entries for the Claude Desktop config (`private_Library/…claude_desktop_config.json`) and `executable_awssso` use source paths, so they don't match — the Claude Desktop config still syncs and awssso isn't gated off non-work machines. Fix to target paths? _(chezmoi ignored lists neither)_
- App-owned managed files (encrypted settings.json / plugin manifests) prompt overwrite/skip on every `chezmoi update`, since chezmoi apply-writes them while the app rewrites the live copy. Cleaner mode wanted (auto-skip), or accept the churn?
- Is `slalom-agent-kit`'s absence intentional? `60637c4` restored it into the plugin manifest on 2026-08-07 after a backwards capture dropped it, and as of 2026-08-08 it is absent from both the source manifest and the live `~/.claude/plugins/installed_plugins.json`, with no marketplace directory on disk. Either it was removed deliberately since, or Claude Code pruned a manifest entry with no plugin behind it and the 22:00 auto-sync captured that. The two manifests otherwise agree and the capture looks correct, so this is a question rather than a known bug. _(dot_claude/private_plugins/, b7beb82)_
- Arrow ligatures (`->`, `=>`) don't render in Ghostty or the VS Code integrated terminal, while the equals family (`==`, `!=`, `===`) does, and everything renders correctly in the VS Code editor. No mechanism found. Ruled out with evidence, so don't re-derive: it isn't font-specific (Fantasque and MonoLisa fail identically), isn't the Symbols fallback stacking (Fantasque's block has no fallback line), isn't the feature string (`calt` alone and `dlig,liga` fail the same way), and isn't missing glyphs — Fantasque carries `hyphen_greater.liga` and `equal_equal.liga` side by side, both reachable only through `calt`, the one substitution feature it exposes. Adding `liga` and `ss01` to the Ghostty block changed nothing. Ghostty is 1.3.1, past the 1.2.0 release that turned ligatures off by default. Next lead if it ever matters: some fonts gate arrows behind a named stylistic set (Monaspace uses `ss03` for `->`, `ss02` for `<=`), so a per-font terminal feature string naming the arrow set may fix it for the fonts that have one. Cosmetic, terminal-only, and accepted as-is for now. _(2026-08-05)_
